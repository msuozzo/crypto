#include<stdio.h>
#include<stdlib.h>
#include "aes.h"


char *encrypt(char *plaintext, int pt_length, char *key, int key_length) {
  char **state = (char **) malloc(1);
  printf("%d\n", Nb);
  return NULL;
}


char *decrypt(char *ciphertext, int ct_length, char *key, int key_length) {
  return NULL;
}
